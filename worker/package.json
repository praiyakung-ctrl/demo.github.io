{
  "name": "chonburi-cctv-email-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker: proxies transactional email sends to Brevo so the Brevo API key never reaches the browser bundle.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "~6.0.2",
    "wrangler": "^4.0.0"
  }
}
